

var typ = prompt("side lenght ?");

function calculation(typ){
    var environment = typ * 4;
    alert("Around the Square: " + environment );

    var area = typ * typ;

    alert("area of Square: " + area);
}

calculation(typ);